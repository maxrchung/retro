{{- if .Values.local }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: maxrchung-retro
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: maxrchung-retro
            port:
              number: 80
{{- end }}